#!/usr/bin/env bash

set -ue

SO_PATH="$(dirname $(realpath "$0"))/../build/Release/publicaddr.node"

set -x

if [[ -f "$SO_PATH" ]]; then
  LD_PRELOAD="${SO_PATH} ${LD_PRELOAD:-}" exec "$@"
else
  exec "$@"
fi
